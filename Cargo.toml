[package]
name = "feature_gates"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]

[dependencies]
tari_feature = { version = "0.1", path = "./tari_feature" }
